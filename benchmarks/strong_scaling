#! /bin/sh

# awk expression, int
aei () { awk 'BEGIN {printf "%d", '"$@"'}'; }

L=288
#name=periodic_poiseuille
name=periodic_blood

script="cases/${name}.py"

for i in `seq 6`; do
    ./launch_daint $i $i $i  $L $L $L  $script $name "strong"
done
